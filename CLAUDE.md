# 小说库相似度比对服务规则

## 1. 项目定位

当前工作目录对应项目：

- 小说库相似度比对服务

工作区位置：

- `E:\点众\小说库相似度比对服务工作区`

这是一个独立服务项目，不是桌面工具项目里的一个功能页。

上游旧项目位置：

- `E:\点众\自动化工具\侵权巡检助手工作区\源码`

旧项目只负责字幕生产和结果提交；本项目只负责小说库相似度比对。

## 2. 本项目当前目标

当前只做这条主线：

- 小说库接入
- 数据清洗
- 索引构建
- 字幕提交
- 候选召回
- 精排比对
- 证据输出

不在本项目里处理：

- 桌面 GUI
- 视频下载
- 音频抽取
- ASR
- 视频分析模型

## 3. 会话开始时先看什么

新会话先看这三个文件，按顺序：

1. `README.md`
2. `docs/00_项目关系说明.md`
3. `docs/01_需求与现状.md`

只有在要落技术方案或开始实现时，再看：

4. `docs/02_基础方案.md`

## 4. 文档和代码的轻量规则

- 新增功能前，先判断它属于“服务侧”还是“桌面工具侧”
- 如果属于桌面工具侧，不在本项目实现
- 如果属于服务侧，优先先补文档边界，再写代码
- 不一次性设计过重架构，先保证样本验证链路跑通
- 不为了“以后可能会用到”提前引入复杂模块

## 5. 当前推荐推进顺序

按这个顺序推进：

1. 明确小说库交付格式
2. 确认最小字段集
3. 做样本导入
4. 做清洗和标准化
5. 做粗召回
6. 做精排和证据输出
7. 最后再和桌面工具联调

## 6. 记录方式

本项目只保留轻量记录，不上重规则体系。

建议使用：

- `README.md`：项目定位和目录说明
- `docs/`：需求、方案、接口、决策文档
- `.codex/progress.md`：简短进度记录

记录要求：

- 只记关键变化
- 只记当前结论、阻塞点、下一步
- 不写大段重复背景

## 7. 什么时候更新进度

发生以下情况时更新 `.codex/progress.md`：

- 完成一个阶段性结论
- 改了方案边界
- 新增关键接口
- 遇到明确阻塞

每次更新只写：

- 今天完成了什么
- 当前卡在哪里
- 下一步做什么

## 8. 质量底线

- 输入输出边界必须清楚
- 结果必须可解释
- 证据必须能落到具体文本片段
- 先做样本验证，再谈全量
- 不把语义分数直接当法务结论

## 9. 遇到边界问题怎么处理

如果发现需求其实属于旧项目：

- 先在文档里注明“应回到旧项目”
- 不在本项目继续实现

如果发现需求横跨两个项目：

- 先定义接口边界
- 再分别落到两个项目

## 10. 当前原则

- 这个项目要轻
- 这个项目要清楚
- 这个项目先做对，再做大

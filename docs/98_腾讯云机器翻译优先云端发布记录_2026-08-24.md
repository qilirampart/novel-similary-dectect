# 腾讯云机器翻译优先云端发布记录

## 发布结果

2026-08-24 已将腾讯云机器翻译优先版本发布到云端小说相似度服务，当前 release：

```text
20260824-tencent-translation-priority-1
```

当前 `current` 指向该 release，服务为 `novel-similarity-api.service`，监听地址仍为 `127.0.0.1:18101`。本次没有修改社区项目，也没有更改 Qdrant collection 或业务数据库结构。

## 环境变量

新增独立 systemd drop-in：

```text
/etc/systemd/system/novel-similarity-api.service.d/20-tencent-translation.conf
```

配置内容包括腾讯云优先顺序、SecretId、SecretKey、`ap-shanghai` 地域、ProjectId `0` 和 TMT endpoint。SecretKey 未写入 release、Git 或本记录。原有 `10-codex-runtime.conf` 未覆盖，DeepL 配置继续保留。

## 备份与回滚

发布前已完成业务 SQLite 一致性备份：

```text
/opt/novel-similarity-service/shared/backups/business-20260824-tencent-translation-priority-1-20260824-191701.sqlite3
```

上一版本：

```text
/opt/novel-similarity-service/releases/20260817-external-budget-gray-4
```

如需回滚，恢复 `current` 到上一版本并重启服务即可。腾讯云环境变量采用独立 drop-in，旧版本即使暂时保留该 drop-in 也不会读取这些未知变量；如需完全撤销，可删除 `20-tencent-translation.conf` 后执行 `systemctl daemon-reload`。

## 发布后验证

- `/api/v1/health/ready` 返回 `200`，业务库、小说检索库和短剧字幕库均为 `ready`。
- systemd 进程正常启动，未发现 `Traceback`、`ERROR` 或启动异常。
- 云端当前 release 使用实际 venv 解释器执行腾讯云翻译 smoke。
- provider 顺序为 `tencent -> deepl`。
- 腾讯云真实翻译调用成功，实际 provider 为 `tencent-tmt-v1`，耗时约 `0.47s`。
- 发布前云端没有小说或短剧活跃任务，未造成任务中断。

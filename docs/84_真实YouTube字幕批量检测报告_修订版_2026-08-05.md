# 真实 YouTube 字幕批量检测报告（修订版）

## 测试范围

测试文件：`E:\点众\YouTube字幕核验助手工作区\output\youtube_subtitles_20260805_101043.xlsx`，使用“字幕”工作表，共 176 条英文字幕。文本长度为 298 至 2478 个字符，平均 1613 个字符。系统使用完整字幕作为查询文本，并保留视频标题、视频 ID、频道、字幕语言和字幕来源等溯源字段。

任务 ID：`898fa489-1e27-4030-bbf5-2bb090d6da08`

## 结果口径

“返回候选”只代表检索链路返回了结果，不能代表召回成功，更不能代表侵权成立。本轮没有提供每条字幕对应的原著书名金标，因此无法直接计算真实 Top1、Top5、Top10 召回准确率。

本轮 176 条中，175 条执行完成，1 条失败，任务执行成功率为 99.43%。175 条完成结果均返回 10 个候选，仅能记为“候选返回率 100%”，不能记为“召回成功率 100%”。

## 匹配强度分层

为判断候选是否具有足够的可解释重合，本次使用两个现有文本指标进行临时分析：证据窗口覆盖率至少 0.60，且共享四词短语至少 20 个，记为“暂定强匹配证据”；低于该条件但仍存在词级证据，记为“中等匹配证据”；只有语义候选、没有词级重合证据，记为“弱匹配证据”。这只是本轮分析口径，不是未经金标验证的产品最终阈值。

按该口径，175 条完成结果中暂定强匹配证据 171 条，中等匹配证据 2 条，弱匹配证据 2 条。171 条只能说明证据窗口存在较强文本重合，仍需在复核页确认命中的书名、集数和完整证据是否对应同一剧情；不能直接称为真实召回成功。

两条中等匹配分别为第 21 条和第 26 条，Top1 证据覆盖率分别为 0.5526 和 0.4889，共享四词短语分别为 42 个和 22 个。两条弱匹配为第 45 条和第 117 条，只有语义候选，没有词级重合证据，语义分数分别为 0.5996 和 0.4847，应优先列为人工复核或低置信候选，不能作为有效命中。

## 耗时与稳定性

175 条成功条目的单条耗时平均 3.345 秒，中位数 2.800 秒，最短 1.208 秒，最长 19.690 秒，P95 为 6.102 秒。词级检索平均 1.605 秒，语义检索平均 1.740 秒；175 条语义调用全部为 `ok`，没有语义降级。173 条同时拥有词级和语义来源，2 条主要由语义来源返回。

唯一失败条目视频 ID 为 `RPKMJAwsfAw`，失败原因是 `Remote end closed connection without response`，单条耗时 26.733 秒，属于外部语义服务连接中断，不是 Qdrant 无数据。该条应通过重试机制重新执行后再纳入最终质量统计。

任务从 10:30:25 开始，到 10:54:44 完成，其中包含电脑重启造成的中断。重启后已完成的 115 条结果被保留，剩余条目继续执行，说明结果落库和断点数据可用；但系统没有自动把遗留的 `running` 短剧任务恢复为可消费状态，本轮进行了手动断点修复。这是当前需要修复的任务恢复隐患。

## 结论

本轮可以确认：真实英文字幕能够被系统正确解析，词级检索、同语言 Qwen3-Embedding-4B 语义召回和证据落库链路均可运行，稳定处理速度约为每条 3.3 秒。本轮不能确认 171 条全部召回正确原著，因为缺少人工金标；最终有效召回率必须以“正确原著 + 足够重合证据”的人工复核结果计算。

后续优先增加失败条目重试、任务重启自动恢复，并建立真实字幕金标集，分别统计“正确命中、相近误召回、无关候选”和 TopK 召回率。Reranker 暂不接入默认链路。

# 封面云端存储方案

更新时间：2026-09-13。当前状态：S1、S2、S3 已完成，S4 生命周期进行中。

## 结论

封面图片不应长期保存在应用发布目录，也不应写入 SQLite。云端正式方案采用“私有 OSS 持久化 + 服务器临时审核缓存 + SQLite 保存元数据和对象键”。本地开发继续使用文件系统后端，业务层只依赖统一的封面存储接口。这样既不会让每次 release 重复携带图片，也不会因发布、回滚或服务器磁盘清理丢失侵权证据。

OSS Bucket 必须保持私有。页面查看证据时，后端先校验用户、工作空间、案件和资产归属，再通过应用服务代理读取 OSS 对象；数据库和前端都不保存永久公开 URL，也不把 OSS 内网地址或签名地址暴露给浏览器。ECS 上优先使用实例 RAM 角色取得自动轮换的临时凭证，避免在代码或环境文件中长期保存 AccessKey。

## 数据流

采集器只负责得到 YouTube 封面地址并保存来源 URL。来源 URL 是外部 CDN 地址，只用于追溯和重新采集，不能代替证据文件。下载阶段把图片写入服务器临时目录，完成最终跳转域名、MIME、尺寸、像素、文件大小和 SHA-256 校验后，以内容哈希作为不可变版本写入 OSS。视觉模型读取这份已经校验且完成留证的相同字节，避免“模型看到的图片”和“系统保存的证据”发生变化；SQLite 仅记录来源 URL、`storage_backend`、`storage_key`、内容哈希、媒体类型、尺寸和大小。

建议对象键采用：

```text
cover-monitor/v1/workspaces/{workspace_key}/videos/{video_id}/{sha256}.{ext}
```

同一视频换封面会产生新的哈希对象，不覆盖旧证据；相同内容重复下载时复用同一对象。`workspace_key` 与 `video_id` 必须经过服务端规则校验，不能直接信任外部路径。OSS 上传成功后才允许数据库把资产标记为可用；数据库写入失败时对象可以由孤儿对象清扫任务回收，不能反过来先删本地文件再尝试上传。

## 保留与清理

风险、待复核、整改前后版本和已进入案件时间线的封面属于证据，不参与普通短周期清理。普通安全封面建议保留 60 天，每个视频至少保留最新版本；下载成功但模型处理失败的封面保留 14 天用于重试；服务器临时审核文件成功处理后立即删除，异常退出遗留文件超过 24 小时清理；导出报告保留 30 天。清理采用先生成清单、再标记、最后删除的流程，并记录操作者、对象键、原因和时间。

OSS 生命周期可按对象前缀或标签执行转低频、归档或删除，但规则加载和执行并非实时。因此案件证据不使用简单的统一到期规则，而应通过业务状态打标签或使用独立前缀保护。确有法务不可篡改要求时，再单独评估 WORM 保留策略，不能直接对整个 Bucket 开启后再发现历史对象无法删除。

## 容量预估

本次真实样本 10 张共约 246 KiB，平均约 24.6 KiB，但不能以这个小样本作为上限。按每张 100 至 300 KiB 估算，11.4 万条视频每个版本约占 11 至 34 GiB；换图历史、失败重试和导出会继续增加占用。OSS 方案可以把容量增长从云服务器系统盘中剥离，并通过生命周期降低长期成本。

## 配置与安全

计划新增以下配置，配置文件只保留占位符，不提交真实密钥：

```text
COVER_MONITOR_STORAGE_BACKEND=local|oss
COVER_MONITOR_ASSET_ROOT=/srv/novel-similarity/shared/cover-assets
COVER_MONITOR_STAGING_ROOT=/srv/novel-similarity/shared/cover-staging
COVER_MONITOR_OSS_ENDPOINT=oss-cn-xxx-internal.aliyuncs.com
COVER_MONITOR_OSS_BUCKET=private-bucket-name
COVER_MONITOR_OSS_PREFIX=cover-monitor/v1
COVER_MONITOR_OSS_ECS_ROLE_NAME=role-name
```

同地域 ECS 优先使用 OSS 内网 Endpoint，减少公网流量和延迟。实例 RAM 角色仅授予指定 Bucket 前缀所需的 `PutObject`、`GetObject`、`HeadObject` 等最小权限；批量删除和生命周期配置权限不授予应用进程，交给独立运维任务。

## 实施任务板

| 任务 | 状态 | 完成标准 |
|---|---|---|
| S1 定义存储接口和本地文件实现 | 已完成 | 下载、审核、证据访问不再直接拼接资产根目录，现有测试全通过 |
| S2 增加 OSS 实现与配置校验 | 已完成 | 支持上传、存在性校验、临时物化和鉴权后端代理访问，密钥不入库不入日志 |
| S3 数据库兼容存储后端 | 已完成 | 旧记录默认识别为 local，新记录保存 backend，不破坏历史证据 |
| S4 临时目录和对象生命周期 | 进行中 | 有 dry-run 清单、案件保护、失败重试和审计记录 |
| S5 本地到 OSS 迁移工具 | 待开始 | 可断点、可校验哈希、可重复执行，迁移失败不删除本地源文件 |
| S6 云端灰度与回滚 | 待开始 | 新资产先写 OSS，历史资产仍可读；异常时一项配置回退 local |

S2 完成后仍不直接改变生产存储位置。先在独立前缀上传 10 张真实样本，验证来源 URL 留存、重复上传去重、模型审核、登录后证据预览、未登录访问拒绝和临时缓存清理，再开始灰度。

### C9 执行任务板

| 编号 | 状态 | 任务 | 验收标准 |
|---|---|---|---|
| C9.1 | 已完成 | 抽象本地与云端统一存储契约 | 下载器、模型执行器和证据接口不再直接依赖资产目录 |
| C9.2 | 已完成 | 实现私有 OSS 不可变对象写入与读取 | 按 SHA-256 对象键上传，重复内容不重复写入，下载后再次校验哈希 |
| C9.3 | 已完成 | 改为后端鉴权代理交付证据 | 浏览器只访问登录保护的业务 API，不接触 OSS 内网地址或永久 URL |
| C9.4 | 已完成 | 补齐配置模板和启动期校验 | 默认保持 local；OSS 配置不完整时启动失败且不输出密钥 |
| C9.5 | 已完成 | 数据库兼容存储后端并按记录混合读取 | 来源 URL 继续留存；旧记录回填 local，新记录保存 backend，历史 local 与新 OSS 可并存读取 |
| C9.6 | 进行中 | 临时缓存与 OSS 分级生命周期 | 安全 60 天、处理失败 14 天、风险与待复核长期保护，清理先 dry-run 后执行 |
| C9.7 | 待办 | 迁移工具、10 张真实样本灰度与回滚 | 可断点校验、不删除源文件，一项配置可回退 local，完整回归无退化 |

### S1 完成记录

更新时间：2026-09-12。

新增统一 `CoverAssetStorage` 契约，覆盖不可变内容写入、模型审核临时物化和鉴权后证据交付；本地实现 `LocalCoverAssetStorage` 对对象键进行独立校验，阻止绝对路径、反斜杠和 `..` 越界，写入采用同目录临时文件和原子链接，同一对象键不得覆盖不同内容。下载器保持原来的目录参数兼容，同时支持注入存储后端；执行器优先通过存储后端物化图片，旧测试下载器仍可走兼容路径；证据 API 完成案件归属校验后再调用存储交付接口，并拒绝非 HTTPS 的远程交付地址。

本阶段没有修改数据库 schema，也没有切换正式资产目录。现有云端继续使用 local 后端时行为不变，为 S2 接入私有 OSS 保留了明确边界。

### S2 完成记录

更新时间：2026-09-13。

新增阿里云 OSS SDK V2 适配器和 `OssCoverAssetStorage`。新封面先按现有下载边界完成图片校验，再按 SHA-256 对象键写入临时目录和私有 OSS；已存在对象不重复上传。模型审核或证据查看需要文件时，如果临时副本不存在，会从 OSS 内网下载并重新校验内容哈希；临时副本被截断或篡改时会自动丢弃并从 OSS 恢复，不会把错误字节交给模型或用户。

证据接口保持原有登录、工作空间、案件和资产归属校验，校验通过后由应用服务返回本地物化文件，不向浏览器暴露 OSS 地址。部署依赖已固定在 `requirements-deploy.txt`，`.env.cover.example` 只包含占位符；默认后端仍为 `local`，缺少 OSS 配置不会影响现有环境。真实 SDK `alibabacloud-oss-v2 1.4.0` 的请求结构和方法签名已验证，封面专项回归 `101 passed`，项目全量回归 `228 passed`，Python 编译和 `git diff --check` 通过。

本阶段仍未修改数据库 schema，也未切换云端配置。切换到 OSS 前必须先完成 S3，使历史 local 记录和新 OSS 记录能够同时读取，避免启用全局后端后历史证据失效。

### S3 完成记录

更新时间：2026-09-13。

封面数据库 schema 升至 v7，`cover_assets` 新增受约束的 `storage_backend` 字段。新库直接按 v7 建表，已有数据库在初始化时以增量迁移增加字段，所有历史资产自动得到 `local` 默认值；新下载结果从实际存储实例取得后端名称并随资产元数据落库，非法后端值会在业务层和数据库层同时拒绝。

启用 OSS 时，应用构建以 OSS 为写入端、同时注册 local 与 OSS 的混合存储路由。下载器只向当前活动后端写入；模型读取、失败重试和案件证据查看则依据资产行记录的后端选择文件来源，因此切换后不会把历史 local 对象错误地交给 OSS 查询。来源 URL、最终下载 URL 和内容哈希仍按原契约保留。

相同内容在历史上仅保存于 local、随后成功写入 OSS 时，资产元数据会单向升级为 `oss`，避免数据库唯一键继续指向可能已经丢失的本地文件；后续 local 重放不会把已确认的 OSS 记录降级。验证覆盖 v6 旧库迁移、迁移幂等、新资产后端校验、OSS 活动写入、单向升级、历史 local 模型物化和登录后案件证据读取；封面专项回归 `107 passed`，项目全量回归 `234 passed`，Python 编译和 `git diff --check` 通过。云端配置仍保持 `local`，没有执行生产迁移或对象上传。

### S4 子阶段：持久资产 dry-run 清单

状态：已完成。更新时间：2026-09-13。

新增纯判定模块和 `scripts/plan_cover_asset_cleanup_v1.py`，按分页方式读取资产并输出 UTF-8 CSV，不提供删除或应用参数。判定优先保护案件时间线证据、风险/待复核/未知检测以及每部视频最新资产；其余安全资产超过 60 天、无检测结果资产超过 14 天才列为 `delete_candidate`。时间、后端、大小或保护标志异常时统一保守保留为 `invalid_metadata`，单条坏数据不会让整批证据误删。

数据库 schema 升至 v8，增加资产生命周期、检测资产和案件事件索引，避免十万级资产清单中的关联判断退化。隔离空库端到端验证已生成 CSV，并明确输出 `mode=dry_run`、候选数量和预计可释放字节；项目全量回归 `239 passed`。本子阶段没有删除本地文件或 OSS 对象，也没有运行生产库清单。

S4 下一子阶段：生成 staging 临时缓存 24 小时 dry-run 清单，并设计带清单摘要、操作者、执行时间和逐项结果的审计记录；在两类清单复核完成前不开放实际删除。

### S4 子阶段：staging 临时缓存 dry-run 清单

状态：已完成。更新时间：2026-09-13。

新增 `scripts/plan_cover_staging_cleanup_v1.py`。计划器只识别封面下载器和 OSS 物化流程实际生成的内容哈希图片与 `.part-*` 下载分片；超过 24 小时才列为候选。目录中的陌生文件、符号链接、异常元数据和不符合视频 ID/哈希规则的路径一律保留，不跟随链接、不扩大扫描根目录。输出仍为 UTF-8 CSV 和 `mode=dry_run` 汇总，不执行删除。

下一子阶段为清理审计与受控执行：执行必须绑定一份未被修改的 dry-run 清单，重新核验路径、哈希和数据库保护状态，逐项记录跳过、删除或失败结果；默认继续只允许 dry-run。

### S4 子阶段：清理审计数据结构

状态：已完成。更新时间：2026-09-13。

封面数据库 schema 升至 v9，新增清理计划和候选项审计表。每次计划记录工作空间、清理类型、清单路径与 SHA-256、策略快照、总记录数、候选数、预计释放字节和创建人；候选项初始只能写入 `pending`，并为后续跳过、删除、失败结果预留逐项状态。登记接口拒绝非法摘要、重复候选、负数大小和候选数超过总数等异常输入。

本阶段只建立 `planned` 计划登记能力，没有审批、执行或删除入口。下一步由独立命令校验 dry-run CSV、计算原始文件摘要并登记计划，之后再实现执行前二次保护校验。

### S4 子阶段：清单摘要绑定

状态：已完成。更新时间：2026-09-13。

持久资产与 staging 清单生成器均支持可选的 `--register-audit`。生成器在 CSV 完整落盘后计算原始文件 SHA-256，并使用本次实际保留策略和候选项登记 `planned` 计划，避免人工二次录入策略造成偏差；staging 命令只有同时显式提供数据库路径时才允许登记。默认不启用该参数时仍只输出 dry-run CSV。

同一工作空间、清理类型和清单摘要重复登记时幂等返回原计划；若摘要相同但策略、统计或候选项发生变化则拒绝登记。当前仍没有审批状态迁移、文件删除或 OSS 删除能力。下一步实现只读预检，对已登记清单重新计算摘要，并针对实时数据库保护状态和 staging 路径边界逐项给出可执行或跳过结论。

### S4 子阶段：staging 执行前只读预检

状态：已完成。更新时间：2026-09-13。

新增只读预检服务，按工作空间读取已登记计划并重新计算清单 SHA-256。运行时必须显式传入当前 staging 根目录，且必须与计划中的策略快照一致，防止审计记录把扫描范围引向其他目录。候选路径需要再次通过系统命名规则、目录边界和符号链接检查；文件必须仍存在、仍超过保留期且大小未变化，正式哈希图片还会重新计算内容 SHA-256。同尺寸篡改、清单变化、目录变化或跨工作空间访问都会阻断或逐项跳过。

该预检只返回 `ready` 与 `skipped` 结论，不更新审计状态、不移动文件，也不删除本地或 OSS 对象。下一步为持久资产增加同等级的实时数据库保护复核，确保候选在计划生成后若成为最新封面、风险检测或案件证据会被自动跳过。

### S4 子阶段：持久资产执行前只读预检

状态：已完成。更新时间：2026-09-13。

持久资产预检按工作空间和资产 ID 重新读取实时生命周期状态，并复用 dry-run 使用的同一判定器和同一策略快照。候选若已不存在、字节数变化、保留原因变化，或在计划生成后成为每部视频最新封面、风险/待复核/未知检测关联资产、案件时间线证据，都会逐项标记为 `skipped`。测试已覆盖原候选正常放行、计划后成为最新封面以及计划后成为案件证据三种关键路径。

至此，两类存储都具备清单摘要绑定和只读二次保护校验，但仍没有实际删除入口。下一步设计审批状态迁移和执行令牌；真正执行时必须在删除每一项之前再次运行相同校验，不能把本阶段的预检结果当作长期有效授权。

### S4 子阶段：显式审批与短时执行令牌

状态：已完成。更新时间：2026-09-14。

封面数据库 schema 升至 v10，清理计划增加执行令牌哈希和到期时间，已有 v9 数据库通过增量加列迁移。审批服务要求计划仍为 `planned`、调用方再次提供完全一致的清单 SHA-256，并先完成实时只读预检；至少存在一项 `ready` 候选时才允许原子迁移到 `approved`。令牌默认有效 15 分钟，可配置范围限制为 1 至 60 分钟。

执行令牌使用高熵随机值，只在审批响应中返回一次，数据库仅保存 SHA-256；普通计划查询主动移除令牌哈希。重复审批、跨工作空间审批、清单被修改或摘要不一致都会拒绝。当前尚无令牌消费和删除入口，下一步实现一次性领取：校验令牌和有效期后原子迁移到 `running` 并立即清空令牌哈希，然后逐项重新预检。

### S4 子阶段：执行令牌一次性领取

状态：已完成。更新时间：2026-09-14。

新增事务化领取接口。调用方提交原始执行令牌后，服务仅比较其 SHA-256；计划必须属于当前工作空间、状态为 `approved` 且令牌未过期。领取成功时原子迁移为 `running`，记录开始时间，并立即清空令牌哈希和到期时间，因此同一令牌无法再次启动执行。错误令牌不会改变审批状态；过期令牌会把计划关闭为 `cancelled` 并清除授权，必须重新生成和审批清单。

领取动作本身仍不删除任何内容。`running` 计划允许再次执行只读预检，下一步实现逐项结果登记和仅支持隔离 staging 目录的受控执行器；OSS 与正式持久资产删除继续保持禁用。

### S4 子阶段：逐项结果审计事务

状态：已完成。更新时间：2026-09-14。

新增逐项结果登记与计划结束接口。只有当前工作空间内处于 `running` 的计划可以把 `pending` 候选写为 `deleted`、`skipped` 或 `failed`；首次结果会固化消息和执行时间，相同结果重放保持幂等，任何对既有结果的改写都会拒绝。仍有 pending 项时禁止结束计划，全部结束后按是否存在 failed 汇总为 `completed` 或 `partial_failed`。

本阶段只修改审计记录，没有文件操作。下一步实现 staging 专用执行器：领取令牌后重新预检，每个 ready 项在调用 `unlink` 前再次校验并立即记录结果；持久资产和 OSS 删除仍不实现。

## 官方依据

- [阿里云 OSS 私有对象与签名 URL](https://help.aliyun.com/en/oss/use-a-fixed-file-url-to-access-a-file)
- [阿里云 OSS Python SDK 凭证配置与 ECS RAM 角色](https://help.aliyun.com/en/oss/python-configuration-access-credentials)
- [阿里云 OSS 生命周期管理](https://help.aliyun.com/en/oss/user-guide/overview-54/)
- [阿里云 OSS 保留策略](https://help.aliyun.com/en/oss/user-guide/oss-retention-policies)

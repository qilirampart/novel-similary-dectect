# 现有字幕数据全链路验证结果

## 验证目的

使用当前短剧字幕库中的真实字幕窗口，模拟 YouTube 工具生成带视频溯源字段的批量 CSV，验证从批量输入、词法召回、任务持久化、复核标记到 Excel 导出的完整流程。

## 验证数据

本次抽取同一部短剧的 3 个真实字幕窗口，来源为 `data/drama_subtitle_similarity_v1.sqlite3`：

- `bookId`: `41000100291`
- 剧名: `My Stunning Boss Lady`
- 字幕语言: `en`
- 模拟视频 ID: `local-test-video`
- 每条数据均携带片段序号、字幕起止时间和原始台词。

## 验证结果

| 项目 | 结果 |
|---|---|
| 批量输入条数 | 3 |
| 任务状态 | completed |
| 成功处理 | 3 |
| 未召回 | 0 |
| 命中 bookId | 41000100291，3 条全部一致 |
| 单条耗时 | 2.2932 秒、2.5976 秒、2.6074 秒 |
| 复核写入 | 成功，首条标记为 confirmed_high_risk |
| 复核 Excel 导出 | 成功，包含视频溯源字段和字幕证据 |

## 结论

当前系统已经能够接收带 YouTube 视频溯源信息的字幕分段批量数据，并完成真实字幕的词法召回和后续复核导出。此次验证未调用 YouTube 在线采集，使用的是本地已有字幕库，因此验证重点是匹配系统与新批量字段的兼容性；下一步应使用真实 YouTube 导出的多 cue 文件再跑一次端到端验证。

可复现脚本: `scripts/smoke_test_existing_subtitle_full_flow_v1.py`

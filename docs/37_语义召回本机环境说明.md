# 语义召回本机环境说明

更新时间：2026-05-07

## 当前主环境

- 本机 Qdrant：`http://127.0.0.1:6333`
- 语义 embedding：当前默认走 `Gitee Embedding API`
- 默认模型：`Qwen3-Embedding-8B`
- 默认 chunk collection：`novel_semantic_chunk_embeddings_qwen3_8b_1024_v1`
- 默认 chapter collection：`novel_chapter_embeddings`

## 结论

- 当前项目的默认语义召回链路，应理解为“本机 Windows 负责运行脚本和服务，本机 Qdrant 负责向量库，embedding 通过 API 完成”。
- `192.168.97.154` 这一组 Mac 地址，只能视为历史实验环境或备用节点，不应再作为当前默认环境。

## 已对齐的入口

- `service/semantic_retrieval.py`
- `scripts/semantic_retrieve_v1.py`
- `scripts/run_global_compare_v1.py`
- `scripts/evaluate_semantic_retrieval_v1.py`
- `scripts/embed_to_qdrant_v1.py`
- `scripts/build_semantic_chunks_v2.py`

## 后续验证建议

1. 先确认本机 `127.0.0.1:6333` 已经启动并且 collection 存在。
2. 再用 `rewrite` 模式复跑未命中的 3 条样本，确认语义候选是否真正进入 merged recall。
3. 若仍未召回，再拆分为“Qdrant 数据问题”和“召回策略问题”分别排查。

# 云端磁盘安全清理记录

## 清理结论

2026-09-09 对小说与字幕匹配服务执行第一批保守清理。清理范围仅包含已经完成合并的上传分片、中断上传残留、冒烟测试数据库和非当前历史发布包，没有修改在线 SQLite 数据库、Qdrant `storage`、当前发布版本、业务备份或可用快照。

本次实际删除 `18,606,246,218` 字节，约 `17.33 GiB`。服务器根分区使用率由 `67%` 降至 `61%`，可用空间由 `95.48 GiB` 增加到 `112.81 GiB`；项目目录由约 `95.27 GiB` 降至 `77.94 GiB`。

## 已清理内容

| 类型 | 路径 | 清理依据 |
| --- | --- | --- |
| 完整文件的上传分片 | `/opt/novel-similarity-service/shared/data/drama_subtitle_similarity_v1_20260902.sqlite3.parts` | 分片总字节与已合并 SQLite 完全一致 |
| 完整文件的上传分片 | `/opt/novel-similarity-service/shared/data/drama_subtitle_similarity_v1_first7_20260904.sqlite3.parts` | 分片总字节与当前完整 SQLite 完全一致 |
| 中断上传残留 | `/opt/novel-similarity-service/shared/data/drama_subtitle_similarity_v1.sqlite3.parts` | 仅约 59 MiB，已长期未更新，完整数据库已存在 |
| 完整快照的上传分片 | `/opt/novel-similarity-service/shared/qdrant_snapshots/drama_subtitle_window_embeddings_qwen3_4b_2560_v1-20260903.snapshot.parts` | 分片总字节与已合并快照完全一致 |
| 冒烟测试数据 | `/opt/novel-similarity-service/shared/data/novel_similarity_v2_pipeline_smoketest.sqlite3` | 未被服务配置引用 |
| 非当前历史发布 | `/opt/novel-similarity-service/releases/20260515-review-responsive-sync` | 当前发布链接指向 20260904 版本 |

## 验收结果

- `novel-similarity-api.service`：`active`
- `novel-similarity-qdrant.service`：`active`
- API readiness：`ready`
- 业务库、小说检索库、字幕库：全部 `ready`
- Qdrant：健康检查通过，小说语义、字幕语义和章节向量三个集合均存在
- 当前发布：`20260904-first7-qdrant-telemetry-1`，清理前后未变化
- 外部登录页：HTTP `200 OK`

本轮未处理约 `19.12 GiB` 历史业务库备份、Qdrant 历史快照和旧字幕数据库。这些文件不参与当前请求，但关系到回滚与数据重建，需制定保留数量或完成异地归档后再清理。

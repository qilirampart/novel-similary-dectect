# 云端方案三 10 条测速（2026-09-03）

## 范围

- 云端发布：`20260903-translation-parallelism-2-1`
- 改动：翻译回退保留首个目标语言串行确认，剩余目标语言按 `target_parallelism=2` 有界并行；不降低原语言、语义或内容证据阈值。
- 任务：`a487e0a4-bbbf-47db-a652-a07b8c70ec75`
- 输入：`data_samples/batch_uploads/42条字幕库补充后重跑输入_20260903_speed10.csv`
- 参数：语义检索、翻译回退均开启，`top_k=10`。

## 结果

10 条全部完成，失败 0。每条结果都记录到 `translation_fallback.target_parallelism=2`，说明新并行调度已在云端生效。第 9 条仍返回 `translation_lexical_continuous_alignment_requires_review`，未因速度优化被错误降级为未命中。

| 指标 | 云端结果 |
| --- | ---: |
| 单条耗时总和 | 497.55 秒 |
| 单条平均耗时 | 49.75 秒 |
| 单条中位数 | 51.93 秒 |
| 最短 | 33.60 秒 |
| 最长 | 61.87 秒 |
| 任务处理墙钟时长 | 约 497 秒 |

同一 10 条输入在本地方案三验证中的平均耗时为 19.56 秒、中位数为 18.40 秒。云端本轮明显更慢，不能将本地的 25.5% 提升直接外推为云端实际吞吐结果。

## 阶段拆解

本轮翻译结果均命中云端缓存，累计翻译 API 耗时为 0 秒，因此没有出现额度耗尽、重试或外部翻译超时导致的慢任务。云端主要时间消耗如下：

| 阶段 | 单条平均 | 单条中位数 |
| --- | ---: | ---: |
| 词法预检 | 22.75 秒 | 22.75 秒 |
| 词法通过后的混合检索 | 23.04 秒 | 21.85 秒 |

当前批量任务由一个 worker 串行处理其内部条目，两个自动 worker 可以并行处理不同任务，不会让同一任务的 10 条并行。因此本任务墙钟时长接近逐条耗时总和。

## 结论

方案三已经安全发布并保持判定策略。云端性能问题集中在扩大字幕库后的词法预检与混合检索阶段，后续优化应优先围绕检索范围、候选裁剪和查询执行路径进行，不应通过降低内容证据门槛或缩短翻译超时来换取速度。
